# Frontend Module: finance

Owner: Priyanshi

## Rules
- Work only inside this folder.
- Do not directly import from other modules. Shared UI/utilities should live in src/components or src/lib (or be proposed for extraction).
- Expose public APIs only via this module's index.js.

