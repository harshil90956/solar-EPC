import React from 'react';
import SolarDashboard from './DashboardNew';

// Main Dashboard - Professional Solar OS Dashboard with 3D Charts
const Dashboard = (props) => {
  return <SolarDashboard {...props} />;
};

export default Dashboard;
